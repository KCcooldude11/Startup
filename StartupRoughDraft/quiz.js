const quizContainer = document.getElementById('quiz');
const resulstContainer = document.getElementById('results');
const submitButton = document.getElementById('submit');
function buildQuiz(){}
function showResults(){}

submitButton.Button.addEventListener('click', showResults);